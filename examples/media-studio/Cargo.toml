[package]
name = "yaiko-media-studio-example"
version = "0.1.0"
edition = "2021"

[dependencies]
yaiko-core = { path = "../../yaiko", features = ["persistent-media"] }
tokio = { version = "1.0", features = ["full"] }
serde_json = "1.0"
sqlx = { version = "0.7", features = ["sqlite", "runtime-tokio-rustls"] }
